{
  "name": "adas-guardian-root",
  "version": "1.0.0",
  "description": "ADAS Guardian Root Configuration",
  "scripts": {
    "install-all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm start --prefix backend\"",
    "start": "concurrently \"npm run start --prefix frontend\" \"npm start --prefix backend\"",
    "build": "npm run build --prefix frontend"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
